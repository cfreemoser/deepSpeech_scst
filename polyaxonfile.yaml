version: 1

kind: experiment

environment:
  resources:
    gpu:
      requests: 1
      limits: 1


build:
  dockerfile: dockerfile
    
run:
  cmd: python DeepSpeech.py --n_hidden 2048 --epochs 3 --train_files /data/shared-task/train_data.csv --dev_files  /data/shared-task/dev_data.csv --test_files /data/shared-task/test_data.csv --learning_rate 0.0001 